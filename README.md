# RotaCash: Despesas e Recibos (React Native & Expo)

Projeto completo com SQLite local offline-first e IA Gemini.

## Comandos para rodar e gerar APK:
```bash
npm install
npm install -g eas-cli
eas login
eas project:init
eas build -p android --profile preview
```
